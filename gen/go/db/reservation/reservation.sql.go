// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: reservation.sql

package reservation

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const checkAndInsertIdempotencyKey = `-- name: CheckAndInsertIdempotencyKey :one
INSERT INTO processed_idempotency_keys (
    tenant_id,
    idempotency_key,
    payload_hash
) VALUES (
    $1, $2, $3
)
ON CONFLICT (tenant_id, idempotency_key) DO NOTHING
RETURNING idempotency_key
`

type CheckAndInsertIdempotencyKeyParams struct {
	TenantID       pgtype.UUID
	IdempotencyKey string
	PayloadHash    string
}

func (q *Queries) CheckAndInsertIdempotencyKey(ctx context.Context, arg CheckAndInsertIdempotencyKeyParams) (string, error) {
	row := q.db.QueryRow(ctx, checkAndInsertIdempotencyKey, arg.TenantID, arg.IdempotencyKey, arg.PayloadHash)
	var idempotency_key string
	err := row.Scan(&idempotency_key)
	return idempotency_key, err
}

const getActiveReservationsByOrder = `-- name: GetActiveReservationsByOrder :many
SELECT id, tenant_id, order_id, sku_id, warehouse_id, quantity, status, expires_at, created_at FROM reservations
WHERE order_id = $1 AND status = 'ACTIVE'
ORDER BY id
FOR UPDATE SKIP LOCKED
`

func (q *Queries) GetActiveReservationsByOrder(ctx context.Context, orderID string) ([]Reservation, error) {
	rows, err := q.db.Query(ctx, getActiveReservationsByOrder, orderID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Reservation
	for rows.Next() {
		var i Reservation
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.OrderID,
			&i.SkuID,
			&i.WarehouseID,
			&i.Quantity,
			&i.Status,
			&i.ExpiresAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getExpiredReservations = `-- name: GetExpiredReservations :many
SELECT id, tenant_id, order_id, sku_id, warehouse_id, quantity, status, expires_at, created_at FROM reservations
WHERE expires_at < $1 AND status = 'ACTIVE'
LIMIT $2
FOR UPDATE SKIP LOCKED
`

type GetExpiredReservationsParams struct {
	ExpiresAt pgtype.Timestamptz
	Limit     int32
}

func (q *Queries) GetExpiredReservations(ctx context.Context, arg GetExpiredReservationsParams) ([]Reservation, error) {
	rows, err := q.db.Query(ctx, getExpiredReservations, arg.ExpiresAt, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Reservation
	for rows.Next() {
		var i Reservation
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.OrderID,
			&i.SkuID,
			&i.WarehouseID,
			&i.Quantity,
			&i.Status,
			&i.ExpiresAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getReservation = `-- name: GetReservation :one
SELECT id, tenant_id, order_id, sku_id, warehouse_id, quantity, status, expires_at, created_at FROM reservations
WHERE id = $1
`

func (q *Queries) GetReservation(ctx context.Context, id pgtype.UUID) (Reservation, error) {
	row := q.db.QueryRow(ctx, getReservation, id)
	var i Reservation
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.OrderID,
		&i.SkuID,
		&i.WarehouseID,
		&i.Quantity,
		&i.Status,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}

const insertOutboxEvent = `-- name: InsertOutboxEvent :one
INSERT INTO outbox (
    tenant_id,
    aggregate_type,
    aggregate_id,
    event_type,
    payload
) VALUES (
    $1, $2, $3, $4, $5
)
RETURNING id, tenant_id, aggregate_type, aggregate_id, event_type, payload, created_at
`

type InsertOutboxEventParams struct {
	TenantID      pgtype.UUID
	AggregateType string
	AggregateID   string
	EventType     string
	Payload       []byte
}

func (q *Queries) InsertOutboxEvent(ctx context.Context, arg InsertOutboxEventParams) (Outbox, error) {
	row := q.db.QueryRow(ctx, insertOutboxEvent,
		arg.TenantID,
		arg.AggregateType,
		arg.AggregateID,
		arg.EventType,
		arg.Payload,
	)
	var i Outbox
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.AggregateType,
		&i.AggregateID,
		&i.EventType,
		&i.Payload,
		&i.CreatedAt,
	)
	return i, err
}

const insertReservation = `-- name: InsertReservation :one
INSERT INTO reservations (
    tenant_id,
    order_id,
    sku_id,
    warehouse_id,
    quantity,
    status,
    expires_at
) VALUES (
    $1, $2, $3, $4, $5, $6, $7
)
RETURNING id, tenant_id, order_id, sku_id, warehouse_id, quantity, status, expires_at, created_at
`

type InsertReservationParams struct {
	TenantID    pgtype.UUID
	OrderID     string
	SkuID       string
	WarehouseID string
	Quantity    int32
	Status      string
	ExpiresAt   pgtype.Timestamptz
}

func (q *Queries) InsertReservation(ctx context.Context, arg InsertReservationParams) (Reservation, error) {
	row := q.db.QueryRow(ctx, insertReservation,
		arg.TenantID,
		arg.OrderID,
		arg.SkuID,
		arg.WarehouseID,
		arg.Quantity,
		arg.Status,
		arg.ExpiresAt,
	)
	var i Reservation
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.OrderID,
		&i.SkuID,
		&i.WarehouseID,
		&i.Quantity,
		&i.Status,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}

const updateReservationStatus = `-- name: UpdateReservationStatus :one
UPDATE reservations
SET status = $2
WHERE id = $1
RETURNING id, tenant_id, order_id, sku_id, warehouse_id, quantity, status, expires_at, created_at
`

type UpdateReservationStatusParams struct {
	ID     pgtype.UUID
	Status string
}

func (q *Queries) UpdateReservationStatus(ctx context.Context, arg UpdateReservationStatusParams) (Reservation, error) {
	row := q.db.QueryRow(ctx, updateReservationStatus, arg.ID, arg.Status)
	var i Reservation
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.OrderID,
		&i.SkuID,
		&i.WarehouseID,
		&i.Quantity,
		&i.Status,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}
